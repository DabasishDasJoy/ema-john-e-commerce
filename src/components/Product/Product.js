import React from 'react';
import './Product.css';

const Product = (props) => {
    const {name, seller, price, ratings} = props.product;
    return (
        <div className='product-card'>
            
        </div>
    );
};

export default Product;